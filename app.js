function cifrado(){
    do}
      var resp = prompt(" indique el numero de lo que desea hacer: 1)cifrar - 2)descifrar");
        if(resp !=""){
         if(resp == "1") {
          cifrar()
          } else if (resp == "2")
            descifrar();
            } else {
             alert ("Ingrese una opcion valida");
         }

        while (resp =="" || (resp !="1" && resp!="2"));
        }
